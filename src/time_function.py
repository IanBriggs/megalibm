
from interval import Interval
from numeric_types import NumericType


def time_function(numeric_type: NumericType,
                  points_per_pad: int,
                  repeats_per_time: int,
                  c_function_name: str,
                  c_code: str,
                  range: Interval) -> float:
    return None
